-p - provide passwrod for sudo, allows automation (requires a string that represents password)
-v - enables verbosity mode
-b - backs up current ssh keys and overwrites newly generated keys during installation (default - only backup)
