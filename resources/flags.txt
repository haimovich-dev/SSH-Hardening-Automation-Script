--password - provide passwrod for sudo, allows automation (requires a string that represents password)
--verbosity - enables verbosity mode
--migrate-and-backup-keys - backs up current ssh keys and overwrites newly generated keys during installation (default - only backup)
